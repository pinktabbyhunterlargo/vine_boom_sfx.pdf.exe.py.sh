#!/usr/bin/env python3
import os
import sys
import subprocess
#	we got bloat les gooo

def install()
	"""Python-based installer capable of multithreading."""
inputs = sys.argv[1:]
ityp = inputs[0]

if ityp == "install":
	inputs[0] = []
	
	#	time to check for --flags
	#	trolled
	
for i in range(len(inputs)
	
	subprocess.run(['bash', '../repos/bpm-call.sh', inputs[i]])

	